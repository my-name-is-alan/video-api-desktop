PORT=8787
BIND=127.0.0.1
# Internal-only business service URL. Never expose this value to App clients.
UPSTREAM_URL=
# Optional bearer token for the real backend; never put this in the Tauri app.
UPSTREAM_TOKEN=
ADMIN_TOKEN=replace-with-a-long-random-token
KEY_PEPPER=replace-with-a-long-random-secret
